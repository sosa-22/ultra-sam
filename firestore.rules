rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /workouts/{workoutId} {
      // Allow public read access to the training plan
      allow read: if true;
      // Deny client-side writes to prevent unauthorized modifications to your data
      allow write: if false;
    }
  }
}
